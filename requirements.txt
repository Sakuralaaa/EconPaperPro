# EconPaper Pro 依赖
# 原生 tkinter GUI 版本

# 核心框架
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# OpenAI兼容接口
openai>=1.30.0

# 文档解析
PyMuPDF>=1.24.0
python-docx>=0.8.11

# 文本处理
jieba>=0.42.1

# 工具库
httpx>=0.25.0
tenacity>=8.2.0
loguru>=0.7.0

# 向量数据库 (知识库功能)
chromadb>=0.4.0

# 网页抓取 (文献搜索)
beautifulsoup4>=4.12.0
playwright>=1.40.0
# 备选方案 (如果 playwright 不可用)
# selenium>=4.15.0
